package com.classschedule.widget

/**
 * 桌面小部件「今日课程」。
 *
 * 渲染逻辑在 [ScheduleWidgetBase] / [ScheduleWidgetContent]，这里只保留具体的小部件实现，
 * 方便以后新增其它样式时按同样的方式再挂一个 receiver。
 */
class ScheduleWidget : ScheduleWidgetBase()
